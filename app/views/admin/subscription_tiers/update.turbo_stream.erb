<%= turbo_stream.replace "admin__#{dom_id(@subscription_tier)}" do %>
  <%= render partial: 'line', locals: {subscription_tier: @subscription_tier,  }   %>
<% end %>
<!-- parental updated -->

<!-- flash notices -->
<%= turbo_stream.update "flash_notices" do %>
  <%= render partial: "layouts/flash_notices", locals: {resource: @subscription_tier} %>
<% end %>
