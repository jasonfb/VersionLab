<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">Version Lab</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav align-items-center gap-2">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "Go to App", "/app", class: "btn btn-primary btn-sm" %>
          </li>
          <li class="nav-item">
            <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-light btn-sm" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Login", new_user_session_path, class: "btn btn-outline-light btn-sm" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up Now", onboarding_path, class: "btn btn-primary btn-sm" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<!-- Hero -->
<section class="bg-dark text-white text-center" style="padding: 8rem 1rem 5rem;">
  <div class="container">
    <h1 class="display-4 fw-bold mb-3">Smarter Audiences, Better Emails</h1>
    <p class="lead mb-4 mx-auto" style="max-width: 600px;">
      Version Lab uses Agentic AI to automatically segment your audience and craft email campaigns that convert.
    </p>
    <% if user_signed_in? %>
      <%= link_to "Go to App", "/app", class: "btn btn-primary btn-lg px-5" %>
    <% else %>
      <%= link_to "Sign Up Now", onboarding_path, class: "btn btn-primary btn-lg px-5" %>
    <% end %>
  </div>
</section>

<!-- Features -->
<section class="py-5">
  <div class="container">
    <div class="row g-4 text-center">
      <div class="col-md-4">
        <div class="mb-3"><i class="bi bi-robot fs-1 text-primary"></i></div>
        <h5>AI-Powered Segmentation</h5>
        <p class="text-body-secondary">Our AI agents analyze subscriber behavior and automatically build high-performing audience segments.</p>
      </div>
      <div class="col-md-4">
        <div class="mb-3"><i class="bi bi-graph-up-arrow fs-1 text-primary"></i></div>
        <h5>Data-Driven Campaigns</h5>
        <p class="text-body-secondary">Send the right message to the right people. Improve open rates, clicks, and conversions.</p>
      </div>
      <div class="col-md-4">
        <div class="mb-3"><i class="bi bi-lightning-charge fs-1 text-primary"></i></div>
        <h5>Set Up in Minutes</h5>
        <p class="text-body-secondary">Connect your email list and let Version Lab do the rest. No complex configuration required.</p>
      </div>
    </div>
  </div>
</section>

<!-- CTA -->
<section class="bg-primary text-white text-center py-5">
  <div class="container">
    <h2 class="fw-bold mb-3">Ready to level up your email marketing?</h2>
    <p class="mb-4">Join Version Lab and let AI handle your audience segmentation.</p>
    <% if user_signed_in? %>
      <%= link_to "Go to App", "/app", class: "btn btn-light btn-lg px-5" %>
    <% else %>
      <%= link_to "Sign Up Now", onboarding_path, class: "btn btn-light btn-lg px-5" %>
    <% end %>
  </div>
</section>

<!-- Footer -->
<footer class="bg-dark text-white-50 text-center py-4">
  <div class="container">
    <small>&copy; <%= Date.today.year %> Version Lab. All rights reserved.</small>
  </div>
</footer>
